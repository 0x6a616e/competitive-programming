#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);
    long long a, b;
    while (cin >> a >> b) {
        cout << abs(b - a) << '\n';
    }
    return 0;
}
